datasource CustomerAccountSource:
    python = "tests.fixtures.ops_desk_lab.datasources:resolve_customer_account"
    requires = [StartRequest]
    produces = CustomerAccount
    render = "markdown"
    cache = "run"

datasource RequestIntentSource:
    python = "tests.fixtures.ops_desk_lab.datasources:resolve_request_intent"
    requires = [StartRequest]
    produces = RequestIntent
    render = "markdown"
    cache = "run"
