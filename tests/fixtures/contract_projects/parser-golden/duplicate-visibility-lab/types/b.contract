type DuplicateType:
    value: str

datasource DuplicateSource:
    python = "tests.fixtures.parser_golden:second_duplicate_source"
    requires = [DuplicateType]
    produces = DuplicateType
    render = "markdown"
    cache = "run"

agent DuplicateAgent() -> DuplicateType:
    goal = "Second duplicate agent declaration."
