Use the hosted web-search tool exactly once to find the official OpenAI home
page. Do not use any host function tools. Return a `CurrentFactEvidence` value
that identifies the page, gives today's date as the as-of date, states one
short factual claim supported by the page, and includes its URL as the
citation.
